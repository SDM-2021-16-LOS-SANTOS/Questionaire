spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:****/studentinfo?characterEncoding=utf-8
    username: root
    password: ***********

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: ****
